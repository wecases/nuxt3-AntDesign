import { defineStore } from "pinia";

export default defineStore({
    id: 'user',
    persist: true,
    state: () => ({
        username: 'waset'
    }),
    getters: {

    },
    actions: {

    },
})
